== 3.1 从单核到多核与 NUMA 架构

=== 背景
进入 2000 年代初，半导体行业遭遇了物理规律的迎头痛击——*登纳德缩放定律破产*。

随着晶体管尺寸逼近原子尺度，漏电流呈指数级上升，芯片的静态功耗激增。CPU 频率在冲破 3.6 GHz 关口时（如 Intel Pentium 4 Prescott 核心），热设计功耗（TDP）飙升至 100W 以上，芯片表面热密度接近核反应堆，盲目提升单核时钟频率的道路彻底封死。

与此同时，传统共享总线结构也逐渐达到扩展极限。无论是多个处理器共享同一总线，还是后来多个核心共享内存控制器，随着并发访问不断增加，内存互连逐渐成为新的系统瓶颈。

=== 解决方案
为了延续摩尔定律，整个微架构界被迫发生范式转移：放弃追求单核极速，转向由多个低频、低功耗核心组成的*多核处理器（Multi-Core Processor）*；同时，在互连结构上引入非均匀内存访问（NUMA）架构。

工程师通过降低单核频率和电压，在同一芯片上平铺多个核心，利用并行的粗粒度线程（Thread-Level Parallelism, TLP）来提升整体吞吐量。

同时，每个处理器节点拥有自己的内存控制器以及直接连接的本地内存，因此访问本地内存速度最快，而访问其他节点内存则必须经过处理器之间的高速互连。

具体而言，多个核心集成在同一颗晶片上，各自拥有私有的 L1/L2 Cache，共享 L3 Cache。为了保证各核心看到的内存视图一致，硬件必须强制执行*缓存一致性协议（如 MESI/MOESI 协议）*。

缓存行被标记为 Modified（修改）、Exclusive（独占）、Shared（共享）、Invalid（无效）。当核心 A 试图写入其 Shared 状态的缓存行时，必须通过片上互连网络向全局广播一条“使无效（Invalidate）”信号，强行将核心 B 中对应的缓存行置为 Invalid，之后核心 A 才能执行写操作。

以 AMD Opteron 和 Intel Nehalem（引入 QPI/UPI 总线）为代表。系统被划分为多个 NUMA 节点，每个 Node 包含一组核心和本地内存。核心访问本地内存延迟极低；若要访问其他 Node 的远端内存，则必须通过点对点的高速互连总线（如 QPI）进行跨节点跨路由跳转，访问延迟会显著增加。

=== 系统层级改变
改变了 *CPU 计算硬件层* 与 *Memory 存储层*，并直接对上层操作系统暴露了物理异构性。 

=== 新的问题
随着核心数量增加，多核并发修改共享变量会导致“使无效”广播信号呈几何级数爆炸，吞噬大量的片上互连带宽，导致硬件扩展性在核心数达到数十个时遭遇严重的瓶颈。

软件如果缺乏 NUMA 感知能力，操作系统可能会将进程分配在 Node 0，而将其内存分配在 Node 1。这种持续的远端访存拉高了平均延迟，导致软件性能发生不可预测的剧烈抖动。

更重要的是，大量传统软件仍按照单线程模型设计。即使处理器拥有越来越多的核心，如果程序本身无法拆分为多个能够并行执行的任务，多核带来的算力依然无法被有效利用。

=== 历史影响
多核处理器成为现代计算机的基本形态。今天无论是服务器、桌面还是智能手机，几乎所有处理器都采用多核设计，通过线程级并行持续提升整体吞吐能力。

与此同时，NUMA 已成为高端服务器和超级计算机的标准架构。操作系统、数据库、虚拟机以及 AI 框架都必须具备 NUMA 感知能力，尽量让计算靠近数据，减少远程内存访问。

然而，多核并没有自动带来高性能。随着硬件逐渐具备越来越强的并行能力，真正的瓶颈开始从硬件转移到软件——如何设计能够充分利用数十乃至数百个核心的程序，成为新的挑战。