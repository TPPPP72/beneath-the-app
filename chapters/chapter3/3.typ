== 3.3 编译器基础设施的重构：LLVM 的诞生

=== 背景
进入 2000 年代后，处理器开始快速分化。除了传统的 x86 外，ARM 在移动设备迅速普及，GPU 开始承担通用计算，各种 DSP、向量处理器以及专用加速器不断出现。

与此同时，高级语言不断发展。除了 C/C++ 外，Objective-C、Java 等语言拥有各自的编译体系，而随后出现的 Swift、Rust 等新语言，也希望能够共享成熟的编译基础设施，而不是重新实现整套编译器。

然而，当时主流编译器大多采用高度耦合的整体式设计。语言前端、优化器与目标平台后端紧密绑定，每增加一种语言或一种硬件，都需要大量修改整个编译器。

=== 解决方案
Chris Lattner 与 Vikram Adve 在 2000 年代初提出了 *LLVM（Low Level Virtual Machine）* 架构。LLVM 从根本上改变了编译器的工程范式，将其重构为一个高度模块化、可复用的软件流水线。

LLVM 的核心思想并不是提出一种新的优化算法，而是将编译器拆解为标准化的软件基础设施。

*LLVM 编译流水线：*
1. *前端（如 Clang）：* 仅负责将高级语言（C/C++）翻译成标准通用的 LLVM IR。
2. *中端（Optimizer）：* 这是 LLVM 的核心。大量的优化算法（如死代码消除、循环向量化 Loop Vectorizer、常量折叠）被抽象为一个个独立的 Pass。这些 Pass 仅输入 LLVM IR 并输出优化后的 LLVM IR。这意味着这些优化 Pass 可以被不同语言前端和不同目标平台共同复用。
3. *后端（LLVM Target Code Generator）：* 针对特定的物理芯片（如 x86、ARM、MIPS），进行指令选择、寄存器分配（基于现代线性扫描或改进的图着色算法）以及指令重排，最终吐出目标机器码。

最终，不同语言共享同一种 IR，不同硬件共享同一套优化 Pass，而各个平台只需实现自己的代码生成后端。

=== 系统层级改变
彻底统治并重塑了*编程语言与编译器层*。

=== 新的问题
LLVM IR 过于偏向低级（Low-Level），它将程序完全扁平化为基本块和纯粹的指针/标量操作。当硬件演进到更高的维度（如需要大块矩阵运算、或者特定的多维数组搬运）时，LLVM IR 已经丢失了高级语言中的高阶几何语义（如无法轻易辨识出这是一个三维张量的卷积操作），导致其很难进行高级别的图级或领域特定的编译优化。

=== 历史影响
LLVM 将编译器从面向特定语言和目标平台的工程实现，逐渐演变为现代软件基础设施。今天，无论是 Clang、Rust、Swift，还是 Apple 的 Xcode、Android NDK、WebAssembly 等，都建立在 LLVM 生态之上。

更重要的是，LLVM 确立了"前端—中端—后端"的模块化架构，使语言、优化算法和硬件后端能够独立演进。这种解耦极大降低了支持新语言和新处理器的成本，也让编译器第一次成为连接软件生态与硬件生态的公共平台。

当新的处理器架构出现时，硬件厂商不再需要重新开发整套编译器，只需实现 LLVM 后端，即可复用成熟的语言前端与优化器。

然而，随着 GPU、AI 加速器以及领域特定架构不断出现，LLVM IR 对张量、并行图和高层语义表达能力的不足逐渐显现。这也推动了 MLIR 等新一代编译基础设施的诞生，为异构计算时代的编译器演化揭开了新的篇章。