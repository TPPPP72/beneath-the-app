== 3.2 操作系统与编程模型的阵痛：线程、同步与内存模型

=== 背景
随着处理器进入多核时代，软件开发首次不得不真正面对并发问题。传统的单线程程序在多核处理器上不仅无法获得性能提升，反而会因为系统调度开销而变慢。

- 多核处理器出现后，传统的软件开发模型仍以单线程为中心。虽然操作系统早已支持多线程，但大多数应用并没有充分利用多个核心，线程之间如何安全、高效地共享数据，成为新的核心问题。
- 现代多核 CPU（如 ARM、x86）为了追求极致的微架构性能，允许读写指令在硬件流水线中乱序执行（Store Buffer、Invalidate Queue 的引入）。这意味着在硬件层面，核心 A 写入数据 $X, Y$ 的顺序，在核心 B 看来可能是颠倒的。这直接摧毁了高级语言中基于顺序一致性（Sequential Consistency, SC）假设编写的并发代码。

=== 解决方案
操作系统全面发展线程模型，并持续演进调度算法，以适应不断增长的核心数量。

同时工业界在 ISO C++ 和 Java 标准中确立了*软件内存模型（Software Memory Model）*，将硬件的乱序特性收敛为可控的语义。

线程成为现代软件开发的基本并发单元。多个线程共享同一进程的地址空间，大幅降低了任务协作和上下文切换的成本。

操作系统为多个核心分配可运行线程，并尽量保持线程与本地 Cache、NUMA 节点的亲和性，以减少迁移带来的性能损失。

为了给程序员提供一种能够跨平台描述并发语义的方式，编程语言开始将并发规则写入语言标准。当软件编写了 Acquire-Release 语义时，*编译器*被禁止跨屏障重排指令；同时，*编译器前端*在生成机器码时，会针对不同硬件插入对应的物理内存屏障指令（如 x86 的 `mfence`/`lock` 前缀，ARM 的 `dmb`）。这些内存屏障约束了编译器和处理器的重排行为，并保证共享数据按照语言规定的内存顺序对其他线程可见。

=== 系统层级改变
重构了*操作系统层（Scheduler/Thread）*。编程语言层建立了统一的软件内存模型，编译器与硬件共同保证并发程序的可见性与顺序语义。

=== 新的问题
为了保证并发安全，软件不得不大量依赖锁。然而锁会导致线程阻塞、引发操作系统上下文切换开销；或者引发惊群效应、激烈的锁竞争。程序中无法并行的临界区直接决定了整体多核系统的性能上限。

死锁、活锁、数据竞争和优先级反转成为了 C++/Java 开发者的噩梦，软件调试与验证成本呈指数级上升。

=== 历史影响
线程与软件内存模型的确立，使并发编程成为现代软件开发的基础能力。今天，无论是 Linux、Windows，还是 Java、Go、Rust 等语言，都建立在统一的线程模型和内存模型之上。程序员无需直接面对不同处理器的乱序执行细节，而是通过原子操作、锁以及内存顺序等抽象构建正确的并发程序。

与此同时，并发也成为软件性能优化的核心课题。随着处理器核心数量不断增长，数据库、Web 服务、分布式系统以及 AI 框架都越来越依赖多线程并行来释放硬件性能。如何降低锁竞争、减少同步开销、提高线程调度效率，逐渐成为现代系统软件的重要研究方向。

从这一时期开始，并发程序设计不再只是操作系统研究的问题，而成为硬件、操作系统、编译器、编程语言与运行时共同演化的一项系统工程。