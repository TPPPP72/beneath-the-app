== 5.1 领域特定架构（DSA）与 Tensor Core 的爆发

=== 背景
随着深度学习特别是 Transformer 架构的兴起，计算负载开始高度集中于矩阵乘加（GEMM）等少数几类运算。相比传统科学计算，AI 模型中的绝大多数时间都消耗在重复的大规模张量计算上。

然而，GPU 本质上仍然是一种通用处理器。虽然拥有极强的数据并行能力，但它仍然需要通过大量通用指令完成矩阵运算，每一步都要经过取指、译码、寄存器访问等完整执行流程。对于高度重复的 AI Workload 而言，这些控制逻辑既无法提升计算结果，却持续消耗着晶体管面积和功耗。

与此同时，制程工艺逐渐逼近物理极限。继续依靠增加晶体管数量或提高频率获得通用算力增长已经越来越困难，而 AI 模型规模却仍以指数速度增长。计算体系结构再次来到新的转折点。

=== 解决方案
工业界开始重新思考一个问题：*是否仍然需要所有计算单元都保持通用？*

John Hennessy 与 David Patterson 提出的*领域特定架构（Domain-Specific Architecture，DSA）*逐渐成为新的设计理念。相比追求"什么都能做"，DSA 更强调针对某一类计算进行极致优化，以牺牲通用性换取数量级的能效提升。

这一思想最具代表性的成果，就是 NVIDIA 在 Volta 架构中引入的 *Tensor Core*。

Tensor Core 不再把矩阵乘法拆解为大量独立的乘法和加法指令，而是直接将矩阵乘加作为一条硬件指令执行。与此同时，它采用 FP16、BF16、INT8 等低精度数据格式，大幅减少数据搬运和计算开销，使单位面积能够集成更多计算单元。

与此同时，Google TPU 等 AI 加速器重新采用了早期提出的*脉动阵列（Systolic Array）*。矩阵数据像流水一样在二维处理单元之间连续流动，每份数据能够在阵列内部反复复用，显著降低了寄存器访问和片外存储带来的能耗。

=== 系统层级改变
彻底重构了 *CPU/GPU 内的核心计算单元微架构层*。

=== 新的问题
领域特定架构获得了惊人的能效提升，却也失去了通用处理器的灵活性。当 AI 模型不断演进，从 CNN 到 Transformer，再到 MoE、稀疏计算等新架构时，固定设计的专用计算单元很难始终保持最高利用率。

更重要的是，当 Tensor Core 将计算能力提升数倍甚至数十倍后，系统瓶颈开始迅速从计算转移到数据供给。显存带宽和处理器互连第一次成为限制 AI 系统性能的决定性因素，整个系统由 Compute-Bound 转向 Memory-Bound。

=== 历史影响
领域特定架构的兴起，标志着计算机体系结构重新进入"专用硬件"时代。处理器设计不再一味追求通用性，而是针对特定工作负载构建专门的计算单元，以获得数量级的性能和能效提升。

Tensor Core 成为这一理念最成功的代表。此后，无论是 NVIDIA GPU、Google TPU，还是各类 AI 加速器，都开始围绕矩阵计算设计专用硬件。AI 不再只是运行在 GPU 上的一种应用，而逐渐成为推动处理器微架构演进的核心力量。

与此同时，处理器内部也开始呈现越来越明显的异构化趋势。现代芯片往往同时集成通用计算核心、向量计算单元以及面向 AI 的专用计算单元，由不同类型的硬件共同完成不同类型的任务，而不是依赖单一通用核心承担所有计算。

然而，专用计算单元带来的巨大算力，也迅速暴露出新的系统瓶颈。随着 Tensor Core 等专用单元不断提升计算速度，显存带宽、片间互连以及数据供给能力逐渐取代计算本身，成为限制 AI 系统性能的关键因素。计算机体系结构的发展重心，也开始从"如何计算得更快"转向"如何让数据跟得上计算"。