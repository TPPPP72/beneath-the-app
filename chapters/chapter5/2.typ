== 5.2 突破物理极限的互连与存储：HBM、NVLink 与 CXL

=== 背景
在千亿、万亿参数的大模型时代，单一芯片在物理上已经无法容纳整模模型的权重。计算遭遇了*物理制造与总线带宽的极限双重撞击*。

一颗芯片由于光刻掩膜版的物理限制，最大面积不能超过 858 平方毫米。在这块巴掌大的地方，塞了算力就塞不下显存。

传统的 GDDR 显存通过印制电路板（PCB）的走线与 GPU 连接，受限于寄生电容和引脚数量，带宽在 1TB/s 封顶。而在大模型多卡并联多机训练（Data Parallelism / Tensor Parallelism）时，传统的 PCIe 总线带宽太窄、延迟太高，多卡之间同步梯度（All-Reduce 语义）导致所有 GPU 都在空转等待。

=== 解决方案
工业界和材料学界深度联手，打破传统 PCB 板的物理框架，从*封装层面*、*私有高速互连总线*以及内存池化协议（CXL）三个维度重构了计算系统的空间物理拓扑。

HBM 放弃了传统的 PCB 走线。它将多层 DRAM 芯片通过硅通孔（TSV）技术在垂直方向上堆叠。随后，使用台积电的 CoWoS（Chip-on-Wafer-on-Substrate） 2.5D 封装技术，将 HBM 堆栈和 GPU 计算晶片共同放置在一块极其精密的硅中介层（Silicon Interposer）上。这使得它们之间的连线引脚（Pins）密度暴增数万倍。HBM 借此刷出了惊人的 4.8TB/s - 8TB/s 的物理超高带宽，彻底缓解了内存墙。

NVIDIA 推出私有互连协议 NVLink，放弃了复杂的 PCIe 协议栈。多颗 GPU 之间通过物理上直接打通的 NVLink 通道点对点连接，带宽较同期的 PCIe 提升了一个数量级。配合板载的 NVSwitch 路由芯片，多台服务器内的数百颗 GPU 可以直接组网，构成一个逻辑上的“巨型单机 GPU”。

针对 CPU 与加速器、内存池之间的互连，业界推出了基于 PCIe 5.0/6.0 物理层的 CXL 协议。CXL 引入了 `CXL.mem` 和 `CXL.cache` 协议，允许 GPU 或专用加速器直接以极低延迟、硬件级缓存一致性的方式去读写宿主机的主存，实现了大范围内物理主存的“去中心化池化”。

=== 系统层级改变
重构了 *存储层（Memory/Storage）*、*互连层（Interconnect/Network）*以及*半导体封装物理层*。

=== 新的问题
CoWoS 封装与 HBM 的工序极其繁琐。只要 3D 堆叠中有一层晶片出现细微瑕疵，或者封装时硅中介层发生热形变，整颗价值数万美元的 AI 芯片就会报废，导致产能严重受物理供应链掣肘。

当算力、HBM 和高速互连被高密度压缩在几平方厘米的封装内时，整块芯片的功耗直接飙升至 700W - 1200W 以上。传统的风冷完全失效，迫使整个现代数据中心机房必须全面拆除重构为成本高昂的全液冷基础设施。

=== 历史影响
HBM、NVLink 与 CXL 的出现，标志着计算机体系结构的优化重心开始从单颗处理器内部，扩展到整个计算系统。

随着 AI 算力持续增长，决定系统性能的已经不再只是处理器本身，而是数据能否以足够快的速度到达计算单元。存储、互连、封装以及数据中心网络逐渐成为与计算核心同等重要的组成部分，"数据供给能力"开始成为现代计算平台的新竞争焦点。

与此同时，GPU 之间的边界也开始逐渐消失。通过 NVLink、NVSwitch 等高速互连，多颗 GPU 能够组成统一的计算系统；而 CXL 则进一步打破了 CPU、GPU 与内存之间原本彼此独立的资源边界，使计算资源逐渐朝着共享、池化和统一寻址的方向发展。

至此，现代计算平台完成了从"单颗高性能芯片"向"大规模互连计算系统"的演变。今天的大模型训练已经很少依赖单块 GPU，而更多依靠数百甚至数千块 GPU 协同完成计算。处理器、存储、互连和封装共同决定了整个系统的最终性能。

然而，随着硬件不断朝着异构化和大规模集群演进，软件也不得不面对越来越复杂的硬件结构。如何让编译器理解张量计算、自动完成算子划分、数据布局和跨设备调度，逐渐成为 AI 时代软件基础设施新的核心挑战。