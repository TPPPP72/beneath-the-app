== 1.2 操作系统雏形：从批处理到多道程序设计

=== 背景
随着硬件本身变得稳定且快速，*人机交互的极低效率*成为了整个系统最大的矛盾。

50 年代末，一台 IBM 大型机的租金高达每小时数百美元。程序员需要带着一叠穿孔卡片排队，手动将卡片塞入读卡器，等待机器启动、加载、运行，再从打印机获取结果。

同时，早期物理外设（如机械式的卡片阅读机、磁带机、行式打印机）其电磁器件延迟比晶体管 CPU 慢几个数量级。当程序执行 I/O 操作时，高昂的 CPU 只能处于彻底的空转状态。根据统计，当时 CPU 实际利用率通常只有百分之十几，很多时候甚至更低。

=== 解决方案
为了榨干每一微秒的 CPU 算力，软件工程师（当时通常是硬件厂商的系统程序员）发明了*单道批处理系统（Single-Stream Batch System）*，随后进一步演进为*多道程序设计（Multiprogramming System）*。系统的核心逻辑从“服务人”转向了“服务 CPU”。

当时引入了一个常驻内存的软件模块——监控程序（Monitor，操作系统的鼻祖），将多个用户的作业（Job）连成一条磁带，由监控程序自动连续执行。操作员将堆叠的穿孔卡片通过低端辅助机一次性写入磁带。常驻内存的 Monitor 读取磁带中的第一个作业，将其控制权交给用户程序。用户程序执行完毕后，通过监督调用（Supervisor Call）或陷入（Trap）将控制权交回 Monitor，Monitor 立即加载下一个作业。

随着硬件中断机制逐渐成熟，多道程序设计开始真正发挥作用。内存被划分为多个区域，同时装入多个独立的作业。引入通道（Channel）架构，通道是一个专用的、能执行简单 I/O 指令的小型处理器。当作业 A 发起 I/O 请求时，CPU 向通道下达命令，随后 OS 的调度器（Scheduler）立即将 CPU 分配给作业 B（进程上下文切换）。当通道完成 I/O 数据传输后，向 CPU 发出硬件中断信号，CPU 暂停当前工作，保存现场，由 OS 的中断处理程序将作业 A 标记为就绪态。

=== 系统层级改变
在物理硬件之上，正式催生了*操作系统层（Operating System）*，并首次引入了硬中断（Hardware Interrupt）的抽象，开始孕育出现代"进程（Process）"这一执行实体的思想。

=== 新的问题
- *内存仍然缺乏保护机制：* 在 0.4 中，我们提到早期的控制流由于没有保护机制导致程序间可以覆写，而当时阶段的操作系统仍然是如此。多个作业同时驻留在物理内存中，如果作业 A 包含一个错误的指针硬编码，或者发生数组越界，它不仅可以轻易改写 OS 监控程序的内容，还能擦除邻近的作业 B 的数据，导致整个多道程序系统轰然崩溃。
- *资源争用与死锁：* 多个程序并发运行，它们开始抢占有限的物理资源。如果没有严格的同步和互连协议，系统极易陷入互相等待的死锁泥潭。
- *缺乏交互性：* 批处理虽然提高了吞吐量，但程序员的调试周期拉长到数小时乃至数天（交出卡片，数小时后才能拿到报错打印单），严重阻碍了软件开发效率。

=== 历史影响
今天你在手机上边听音乐边聊微信，或者服务器并发处理数万个网络请求，其底层的核心机制依然是当年奠定的“中断驱动（Interrupt-Driven）”与“上下文切换（Context Switch）”。无论是 Linux 的 `task_struct` 还是 Windows 的线程调度器，其本质都是多道程序设计思想的现代延续——通过精密的时钟中断与状态保存，让有限的 CPU 核心在多个并发实体之间高速穿梭，营造出“多任务同时运行”的完美时空错觉。

当年为了解放 CPU 而发明的专用“通道（Channel）”架构，直接演化成了今天计算机中不可或缺的 *DMA（直接内存访问，Direct Memory Access）* 技术。不仅如此，现代高性能网络编程中大行其道的异步非阻塞框架（如 Node.js、Go 语言的高并发协程、Linux 的 `io_uring`），其核心思想与当年“CPU 丢下 I/O 请求立即切换去喂饱算力”的逻辑如出一辙。

作业之间无保护覆写带来的灾难，迫使计算机科学家与芯片工程师联合发起了一场硬件级的工业革命。为了解决这一历史痛点，后来的处理器彻底引入了*内核态/用户态的特权级划分（Privilege Rings）*，以及内存管理单元（MMU）与虚拟内存（Virtual Memory）技术。今天任何一个普通应用如果企图越界访问操作系统内存，硬件会产生保护异常（Protection Fault），在 Unix/Linux 中通常表现为段错误（Segmentation Fault），这正是硬件为守护多道程序安全而筑起的物理铁壁。