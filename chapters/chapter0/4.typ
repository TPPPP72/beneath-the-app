== 0.4 早期控制流：硬接线、穿孔卡片与第一代“计算流控制”

=== 背景
如何让机器知道“下一步该干什么”？在现代，我们有程序计数器（PC）和译码器，但在 1940 年代初，*控制流的逻辑硬化*是一个巨大的工程噩梦。

像 ENIAC 这样的早期机器，控制流完全是通过物理电缆在不同的执行部件（如加法器、乘法器）之间手动插拔决定的。如果程序里有一个 `if-else` 分支，工程师必须物理上拉两条电缆，一条接 A 电路，一条接 B 电路。修改一次程序需要整个工程师团队手持接线图忙碌两三天，期间机器完全瘫痪。

像哈佛 Mark I 这样的机器，其指令是串行打在外部穿孔纸带上的。机器读一格纸带，执行一条指令。然而，机械纸带机极其缓慢，且由于纸带必须物理上单向向前拉动，程序根本无法支持*循环*和*动态分支跳转*。如果想写一个循环 $100$ 次的程序，工程师必须在纸带上把这段代码物理上复制粘贴 $100$ 遍，或者把纸带两头粘起来做成一个物理圆环，但一旦循环次数改变，整条纸带就得重做。

=== 解决方案
为了摆脱物理接线和纸带的物理折磨，业界开始尝试将“控制逻辑”与“指令表示”进行初步解耦，引入了*指令控制（Instruction Control）*和*条件转移电路（Conditional Move）*。

1944 年，John von Neumann 在参与 ENIAC 项目时，敏锐指出程序不应再依赖外部布线，而应作为数据存放在内存中。他提出，控制指令不应该由外部物理接线来表达，而应该和数据一样，被编码成数字（Opcode），全部塞进上一小节中发明的动态内存（如水银延迟线）中。

控制单元只需要一个物理寄存器（后来的程序计数器 PC）指向内存中的指令地址。取出一个指令编码，通过二级译码电路（Decoder）将其翻译为驱动 ALU 的控制开关，执行完毕后，PC 自动加 $1$。

如果遇到跳转指令（Branch），根据累加器零标志位（Zero Flag）的电平，直接将 PC 强制重写为目标地址，从而在*不需要物理拉扯纸带或插拔电缆的情况下，实现了软件层面完美的循环和分支逻辑*。

=== 系统层级改变
催生了*计算硬件层中的“控制单元（Control Unit）”与第一代“指令集抽象（ISA）”的萌芽*。

=== 新的问题
由于指令和数据在物理上都只是一串无差别的二进制电平，且存放在同一个物理存储器中。如果程序中的指针出错，或者发生越界，CPU 就会把原本是“数据”的内存错误地当作“指令”去取指译码，导致机器执行一堆毫无意义的垃圾随机代码，机器彻底失控。指令和数据共享同一存储空间，使得"程序修改程序"成为可能，同时也埋下了数据破坏程序的隐患。