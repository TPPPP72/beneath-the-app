== 4.3 云计算基础设施的形成：虚拟化、容器与 Kubernetes

=== 背景
进入 2000 年代末，互联网和移动互联网迅速发展，计算资源开始从单台服务器扩展到成百上千台机器组成的数据中心。

然而，当时的软件部署方式仍然建立在"一台应用对应一台物理服务器"的模式之上。为了保证系统隔离，不同业务通常独占服务器，即使大量机器长期处于低负载状态，整体资源利用率依然很低。

与此同时，互联网业务具有明显的流量波动特征。传统依赖采购、部署和配置物理服务器的方式，往往需要数周甚至数月，已经无法满足快速扩容和弹性部署的需求。

计算资源开始从"如何让一台机器跑得更快"，转向"如何高效管理整个数据中心"。

=== 解决方案
工业界首先利用 CPU 提供的硬件辅助虚拟化能力（Intel VT-x、AMD-V），在物理服务器与操作系统之间引入 Hypervisor。Hypervisor 能够同时运行多个相互隔离的虚拟机（Virtual Machine），每个虚拟机拥有独立的操作系统和虚拟硬件，从而显著提高服务器资源利用率。为了降低虚拟化带来的性能损耗，处理器进一步加入 EPT（Extended Page Table）等硬件机制，将客户机物理地址自动映射到宿主机物理地址，使地址转换几乎全部由硬件完成，大幅降低虚拟化开销。

虽然虚拟机解决了资源利用率问题，但每个虚拟机都包含完整的 Guest OS，启动速度慢，占用内存较大。Linux 随后逐步完善了 Namespace 与 Cgroups，将隔离对象从操作系统降低到进程级别。容器共享宿主机内核，却拥有独立的文件系统、网络、进程视图以及资源配额，实现了秒级启动和更高的资源密度。Docker 的出现，使容器第一次成为标准的软件交付方式。

随着容器数量不断增加，单机已经无法满足生产环境需求。Kubernetes 将容器进一步抽象为可调度资源，在整个数据中心范围内统一完成部署、扩容、故障恢复以及负载均衡，使计算资源第一次能够像水、电一样按需分配。

至此，现代云计算的软件基础设施基本形成。

=== 系统层级改变
计算系统首次从"单机操作系统"演化为"数据中心操作系统"。在硬件与操作系统之间形成 Hypervisor，在操作系统之上形成容器运行时，在更高层由 Kubernetes 统一管理整个集群。

=== 新的问题
虽然虚拟化极大提升了资源利用率，但整个数据中心开始变得越来越复杂。CPU、GPU、内存、网络以及存储共同组成异构资源池，如何在整个集群范围内完成资源调度、性能隔离和故障恢复，逐渐成为新的系统瓶颈。与此同时，大规模 AI 训练开始兴起，GPU 之间的数据交换速度远远落后于计算能力，新的互连和存储瓶颈逐渐浮现。

=== 历史影响
虚拟化、容器和 Kubernetes 共同奠定了现代云计算基础设施。今天，无论是 AWS、Azure、Google Cloud，还是企业私有云，几乎都建立在这一软件栈之上。应用程序不再直接部署到某台服务器，而是运行在统一管理的数据中心资源池中。

更重要的是，计算平台的重心开始从单台计算机转向整个集群。现代操作系统、编译器、运行时以及异构计算框架，都必须围绕数据中心这一新的计算平台协同设计，为随后 AI 基础设施时代的到来奠定了基础。