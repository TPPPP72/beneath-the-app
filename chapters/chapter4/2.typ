== 4.2 统一计算设备架构：CUDA 的封神之路

=== 背景
在 2000 年代中期，GPGPU 面临着*生态与编程软件栈的荒漠*。

不同 GPU 厂商和不同代 GPU 的指令集不断变化，开发者很难编写能够长期复用的软件。同时，GPU 缺乏统一的编译基础设施，每种硬件都需要维护独立的软件工具链，使得优化算法、语言支持和目标平台之间难以共享。

硬件厂商频繁更迭底层指令集（ISA），没有类似 x86 这样长久稳定的二进制规范，软件缺乏可以进行深层次代码优化（如寄存器分配、循环转换）的统一编译器中端。

=== 解决方案
NVIDIA 在 2006 年推出了 *CUDA（Compute Unified Device Architecture，统一计算设备架构）*，并对软件开发者免费开放。CUDA 不仅仅是一个 API 库，它是一套横跨编译器、运行时（Runtime）和驱动的完整系统软件生态。

- CUDA 首先建立了一套面向 GPU 的统一编程模型。开发者可以直接使用 C/C++ 编写 Kernel，而无需再借助 OpenGL、DirectX 等图形 API 将计算伪装成图形渲染。
- CUDA 建立了独立于具体 GPU 型号的编译体系。NVCC 将 Host 代码交给 GCC/Clang 等 CPU 编译器处理，而 Device 代码编译为 PTX。PTX 作为稳定的虚拟 ISA，使同一程序能够跨越多代 GPU，通过驱动中的 JIT 最终生成对应硬件的机器码。
- CUDA Runtime 负责 GPU 内存管理、Kernel 启动、设备同步以及 CPU 与 GPU 之间的数据传输，使 GPU 第一次拥有了完整的软件运行时。

与此同时，CUDA 将 GPU 的多级存储层次显式暴露给程序员，包括 Global Memory、Shared Memory 与 Register 等不同层级。程序员能够主动控制数据布局和线程协作，以充分发挥 GPU 的吞吐能力。

=== 系统层级改变
确立了*异构计算编程语言与运行时层*。

=== 新的问题
手工内存管理给开发者带来了繁重负担，开发者必须在代码中频繁插入 `cudaMemcpy` 和 `cudaMalloc`。一旦发生内存泄漏，或者由于多线程异步下指针同步出错，将引发极难排查的显存越界崩溃。

相比于 CPU 动辄数百 GB 的主存，昂贵的显存（GDDR）容量通常只有数 GB 到十数 GB。当面对大规模图计算或超大矩阵时，程序会因为显存溢出（OOM）而无法运行，被迫在 CPU 与 GPU 之间进行频繁的频繁换页，导致性能雪崩。

=== 历史影响
CUDA 第一次将编译器、运行时、驱动和硬件组织为统一的软件栈，使 GPU 从一种专用图形硬件演变为现代计算平台。今天，从深度学习、科学计算到自动驾驶，大量高性能软件都建立在 CUDA 生态之上。

与此同时，CUDA 也形成了与 NVIDIA GPU 深度绑定的软件生态。开发者一旦采用 CUDA，程序便难以直接迁移到 AMD、Intel 等平台，这种生态壁垒也推动了后来 OpenCL、HIP、SYCL 等跨平台方案的发展。