== 4.1 从不可编程图形硬件到 GPGPU

=== 背景
进入 2000 年代后，3D 游戏、高清视频以及科学计算快速发展，系统开始需要处理海量具有相同计算模式的数据。

与此同时，CPU 已经演化成高度复杂的延迟优化架构。为了让单线程运行得更快，处理器投入大量晶体管构建乱序执行、分支预测、大容量 Cache 等控制逻辑。这种设计非常适合复杂程序，却难以高效执行成千上万个彼此独立的相同计算。

而当时的图形加速卡仍采用固定功能流水线，所有计算逻辑都固化在硬件中，只能完成几何变换、光照和光栅化等图形任务。

研究人员逐渐意识到，大量科学计算与图形渲染一样，本质都是大规模数据并行计算。如果图形处理器能够执行任意程序，它将拥有远高于 CPU 的吞吐能力。

=== 解决方案
微架构和图形界联手推动了图形硬件的彻底变革：将固定管线重构为*可编程着色器*，并拆除复杂的控制逻辑，将晶体管全力投入到密集的计算单元中，诞生了通用 GPU（GPGPU）与 SIMT（单指令多线程）执行模型。

GPU 与 CPU 采用了完全不同的资源分配策略。CPU 将大量晶体管用于复杂控制逻辑，以降低单线程延迟；GPU 则将更多晶体管用于堆叠计算单元，只保留满足吞吐需求的控制逻辑和缓存，空出来的面积平铺了成百上千个简单核心（流处理器，Stream Processor）。它将大量线程组织为 Warp（线程束），并在硬件中同时维护远多于执行单元数量的线程状态。当某个 Warp 因访存停顿时，硬件调度器立即切换到另一个已准备好的 Warp 执行，通过大量线程交替运行来隐藏内存访问延迟，而不是依赖 CPU 式的乱序执行。

=== 系统层级改变
首次将计算能力从 CPU 扩展到独立的异构处理器，形成了 CPU + GPU 协同工作的现代计算架构。

=== 新的问题
如果一个 Warp 内的 32 个线程在执行 `if-else` 条件分支时走向了不同的路径（如 16 个走 `if`，16 个走 `else`），由于 SIMT 共享单一控制单元，硬件不得不串行化执行这两个分支。未选中的线程会被硬件自动掩码（Masked Out）挂起，导致 GPU 的实际算力直接腰斩。

GPU 作为外设挂载在 PCIe 总线上，拥有独立的物理显存。计算前必须通过 PCIe 总线将数据从 CPU 主存复制到显存，计算后再复制回来。PCIe 的低带宽使得系统的耗时大量卡在“搬砖”而非“计算”上。

=== 历史影响
GPGPU 改变了“CPU 是唯一计算核心”这一持续数十年的计算模式。GPU 不再只是图形加速器，而成为与 CPU 并列的通用计算设备。

从科学计算、图像处理到机器学习，越来越多的数据并行任务开始迁移到 GPU 上执行。CPU 负责复杂控制逻辑，GPU 负责大规模并行计算，两者协同工作逐渐成为现代高性能计算的标准模式。

然而，仅有能够运行通用程序的 GPU 还远远不够。程序员仍需要使用图形 API，将普通计算伪装成图形渲染流程。GPU 真正成为大众可编程计算平台，还需要一套统一的编程模型和软件生态，这也直接催生了 CUDA 的诞生。