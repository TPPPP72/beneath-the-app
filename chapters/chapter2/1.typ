== 2.1 指令级并行（ILP）的疯狂榨取

=== 背景
20 世纪 70 年代，登纳德缩放定律使处理器频率和晶体管数量持续增长。进入 80 至 90 年代，随着晶体管预算越来越充裕，体系结构工程师开始思考：如何让新增的晶体管真正转化为性能，而不是仅仅堆积在芯片上。

尽管处理器频率不断提升，传统处理器依然主要按照程序顺序逐条执行指令，大量执行部件在多数时间处于空闲状态。

摩尔定律带来了海量的可用晶体管。如果继续沿用串行取指执行的古老模式，芯片上绝大部分晶体管将沦为“死硅”，无法转化为实际的吞吐量。系统急需在*单个核心内*挖掘出本质的并行性。

=== 解决方案
为了彻底榨干单核性能，体系结构工程师在微架构层面引入了一场“空间换时间”的革命：从*标量流水线（Pipeline）*演进到*超标量（Superscalar）*，并最终走向*乱序执行（Out-of-Order Execution, OoO）*与*分支预测（Branch Prediction）*。

*核心思想：*
1. *指令流水线化：* 将一条指令的执行重构为多个在时间上重叠的子阶段（类比工业流水线）。
2. *多发射与动态调度：* 允许 CPU 在同一个时钟周期内发射多条指令，并通过硬件算法动态分析指令间的数据依赖性，打破程序文本固有的先后顺序，只要操作数准备就绪就立即执行。
3. *推测执行：* 遇到条件跳转时，不再空等结果，而是猜测一条路径并提前执行。

硬件将指令拆分为 IF（取指）、ID（译码）、EX（执行）、MEM（访存）、WB（写回）。超标量技术通过复制执行部件（如放置两个 ALU），实现每周期发射多条指令。

IBM 的 Tomasulo 算法首次证明了硬件能够动态调度指令，后来逐渐发展为现代处理器广泛采用的乱序执行（OoO）机制。
1. *重命名（Rename）：* 硬件将 ISA 的逻辑寄存器映射到规模更大的物理寄存器池上，消除伪相关（WAR/WAW）。
2. *保留站（Reservation Stations）：* 指令译码后进入保留站等待。一旦其依赖的操作数被前序指令通过公共数据总线（CDB）广播出来，该指令立即被捕获并送往执行部件，实现乱序执行。
3. *重排序缓冲区（ROB）：* 指令执行完毕后进入 ROB，必须按照程序原本的顺序依次提交，以确保异常处理的精确性。

处理器同时预测"跳不跳"和"跳到哪里"：方向预测器负责预测分支结果，分支目标缓冲（BTB）负责预测目标地址，从而避免流水线停顿。

=== 系统层级改变
彻底重塑了 *CPU 计算硬件层（微架构层）*，对上层编译器和操作系统保持完全的二进制兼容（通过相同的 ISA 封装）。

=== 新的问题
随着频率冲击数 GHz，流水线深度被拉长到 20 级以上（如 Pentium 4 NetBurst 架构）。一旦分支预测失败，ROB 中已经推测执行的数十条指令必须全部作废并清空流水线，造成的时钟周期浪费极其惨烈。

在大量通用程序中，基本块通常较短，控制转移十分频繁。代码中密集的真数据依赖（RAW）和控制依赖限制了 ILP 的理论上限，超标量架构在 4 发射以上遭遇了严重的边际效益递减。

更重要的是，随着频率不断提升，处理器逐渐撞上了"功耗墙（Power Wall）"。功耗与散热迅速逼近物理极限，继续依赖提高主频和加深流水线已经难以获得线性的性能收益，处理器设计逐渐走向新的十字路口。

=== 历史影响
ILP 将现代 CPU 的复杂度推向了新的高度。今天无论是 Intel Core、AMD Zen，还是 Apple M 系列处理器，其内部依然采用流水线、乱序执行、寄存器重命名和分支预测等技术，在保持相同 ISA 的前提下不断提升单线程性能。

与此同时，ILP 也揭示了单核性能提升的天然边界。程序中的数据依赖和控制依赖无法无限消除，而不断加深流水线和扩大乱序窗口又带来了急剧增长的功耗、面积和设计复杂度。当单核能够自动挖掘的并行性逐渐接近极限后，处理器设计开始寻找新的性能增长路径。