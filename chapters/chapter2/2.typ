== 2.2 存储墙的碰撞：Cache Hierarchy 的诞生

=== 背景
当 CPU 借助流水线和高频率一路狂飙时，作为主存的 DRAM 却走向了不同的优化路径。

20 世纪 80 年代到 90 年代，CPU 性能以每年约 60% 的速度提升，而 DRAM 的访问延迟每年仅改善 7% 左右。两者的性能剪刀差呈指数级放大，CPU 访问一次主存需要等待数百个时钟周期。

即便 OoO 拥有强大的指令调度能力，但在长达数百周期的内存延迟面前，一旦关键访存指令长期无法返回数据，后续依赖它的大量指令便无法提交，ROB 很快被未完成指令占满，导致整个乱序执行窗口随之停滞，处理器不得不空等主存返回数据。

=== 解决方案
为了弥合处理器与内存之间巨大的速度鸿沟，体系结构引入了基于时间局部性（Temporal Locality）和空间局部性（Spatial Locality）的*多级缓存层次结构（Cache Hierarchy）*，并在主存端推行了*同步动态随机存取内存（SDRAM / DDR）*。

Cache 建立在程序具有时间局部性（Temporal Locality）和空间局部性（Spatial Locality）的统计规律之上。它被划分为固定大小的缓存行（Cache Line，通常为 64 字节）。与程序员显式管理内存不同，Cache 完全由硬件自动维护，对软件透明。CPU 发起访存请求时，将虚拟或物理地址拆分为 Tag、Index 和 Offset，在 L1 Cache 中进行高速相联查找（Associative Lookup）。如果 Hit 则在数个周期内返回；如果 Miss，则逐级向 L2、L3 乃至主存发出请求。

硬件预取器（Hardware Prefetcher）能够识别连续访问、固定步长（Stride）等访存模式，提前将数据从主存拉入 Cache，进一步降低 Cache Miss 的概率。

Cache 成功隐藏了绝大多数内存访问，但也让处理器越来越依赖"数据是否已经在 Cache 中"。从此以后，程序性能不仅取决于算法本身，也越来越受到内存访问模式的影响。

与此同时，主存技术也不断演进，从异步 DRAM 发展为 SDRAM，再到 DDR SDRAM，通过提高总线频率、双沿传输和预取机制持续提升内存带宽。然而，这些改进主要缓解了吞吐量问题，并没有根本消除 CPU 与 DRAM 之间巨大的访问延迟差距。

=== 系统层级改变
重塑了*存储层*，在 CPU 核心与物理主存之间强行插入了 L1/L2/L3 Cache 软件不可见层。

=== 新的问题
在这一时期的单机多处理器（SMP）系统或即将到来的多核萌芽中，同一份主存数据可能同时被复制在多个 CPU 的私有 L1 Cache 中。一个核心对其进行修改，其他核心如何感知？这直接带来了灾难性的缓存一致性问题。

而且随着数据集规模超越 Cache 容量，Cache Miss 带来的惩罚（Miss Penalty）愈发显著。芯片开始被巨幅的 SRAM 面积所占据（现代 CPU 一半以上的晶体管面积被用于 Cache），导致算力晶体管的分配受到严重挤压。

为了维持多个 Cache 副本的一致性，处理器不得不引入 MESI 等缓存一致性协议，使片上互连和一致性维护逐渐成为新的性能瓶颈。

=== 历史影响
Cache Hierarchy 成为了现代处理器不可或缺的一部分。今天几乎所有通用 CPU，无论是 Intel、AMD 还是 Apple Silicon，都采用多级缓存结构，在软件几乎无感知的情况下自动管理绝大多数数据访问。

与此同时，Memory Wall 至今仍未被真正解决。过去二十多年，处理器不断增加 Cache 容量、改进预取算法、扩大内存带宽，并发展出 HBM、3D 堆叠存储和 Chiplet 等新技术，本质上都在试图缩小处理器与主存之间长期存在的性能鸿沟。Memory Wall 依然是现代高性能计算和 AI 系统面临的核心挑战之一。