== 2.5 时代总结
20 世纪 70 年代至 90 年代，现代计算机系统完成了从"能够运行程序"到"能够高效运行程序"的转变。

随着晶体管数量持续增长，处理器开始利用流水线、超标量和乱序执行不断挖掘指令级并行；然而，CPU 与 DRAM 日益扩大的速度差距又催生了多级 Cache Hierarchy。与此同时，软件规模不断扩大，操作系统通过虚拟内存和 DMA 接管资源管理，编译器则借助 SSA、中间表示和自动优化不断逼近硬件性能极限。

至此，现代计算机形成了一套持续沿用至今的协同分工：
- 微架构负责挖掘硬件内部并行性；
- Cache 与内存系统负责缓解数据访问延迟；
- 操作系统负责统一管理地址空间、设备与资源；
- 编译器负责将高级语言映射为适合底层硬件执行的机器代码。

然而，这一时期几乎所有性能提升都建立在"单核心"假设之上。

流水线、乱序执行、Cache、虚拟内存以及编译器优化，都试图不断榨取单个处理器的潜力。但随着功耗墙、存储墙以及指令级并行理论极限逐渐显现，继续依靠单核已无法维持性能增长。

计算机体系结构因此迎来了下一次重大转折：*从提升单核性能，转向扩大系统级并行*。

多核处理器、缓存一致性、NUMA、GPU 以及异构计算，正是在这一背景下逐渐成为新的主角。

现代计算机已经不再只是"更快的 CPU"，而是微架构、存储系统、操作系统与编译器共同协作的复杂整体。