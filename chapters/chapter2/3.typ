== 2.3 操作系统与现代存储的成熟：虚拟内存与总线变革

=== 背景
20 世纪 80 年代后期至 90 年代，多任务操作系统（如 UNIX、Windows NT）逐渐成为主流，计算机开始从"一次运行一个程序"迈向真正的多任务时代。随着软件规模爆发，系统在资源隔离与外设 I/O 上遭遇了严重的工程瓶颈。

多个大型进程同时运行，使连续物理内存越来越难以分配；与此同时，程序之间共享同一物理地址空间，错误指针或恶意代码可以轻易越界访问其他进程甚至操作系统。

早期的 ISA、EISA 总线采用共享并行总线拓扑，带宽极低，且缺乏统一标准。更致命的是，CPU 在传输大块外设数据（如硬盘读写）时需要全程参与，每一个字节的传输都要占用 CPU 寄存器，导致高频 CPU 被慢速外设死死拖累。

=== 解决方案
操作系统与微架构紧密联手，推出了硬件内存管理单元（MMU）驱动的虚拟内存（Virtual Memory）机制，并彻底重构了 I/O 拓扑，引入 *PCI 总线* 与 *DMA（直接内存访问）*。

操作系统将虚拟和物理内存划分为固定大小的页（Page，通常为 4KB）。CPU 执行程序时产生的地址均为虚拟地址，通过硬件 *MMU（内存管理单元）* 查找常驻主存的页表（Page Table）。为了加速这一过程，微架构内集成了一个高相联度的 SRAM 缓存——*TLB（快表）*。如果 TLB 命中，地址转换几乎不会成为流水线瓶颈；TLB 未命中时，处理器通过硬件页表遍历（Hardware Page Walk）查询页表；若页表项显示页面尚未驻留物理内存，则处理器产生缺页异常（Page Fault）。操作系统接管控制权，将对应页面从交换区（Swap）或文件系统调入内存，更新页表后恢复程序执行。

与此同时，Intel 主导制定 PCI 总线标准，将外设从局域总线中解耦。当进程请求磁盘数据时，CPU 向 *DMA 控制器* 配置源地址、目的地址和长度，操作系统将发起 I/O 的进程置于等待状态，并调度其他进程运行。DMA 控制器接管总线控制权，将数据直接搬运到物理主存，而无需 CPU 逐字节参与传输。传输完成后发出硬件中断，OS 唤醒对应进程。

从虚拟内存到 DMA，这一时期奠定了现代操作系统"管理资源而非直接计算"的角色定位。*CPU 负责执行计算，MMU 负责地址转换，DMA 负责数据搬运，操作系统则负责协调三者之间的协作关系*。

=== 系统层级改变
深层次地重构了*操作系统层*、计算硬件层（引入 MMU/TLB）与 *I/O 互连层*。

=== 新的问题
现代软件（如大型数据库、C++ 复杂对象拓扑）频繁发生随机跳转，导致 TLB 频繁未命中。一次 TLB Miss 引发的 Page Walk 可能需要访问 3 到 4 次主存页表，使得原本就高昂的存储墙惩罚直接翻了数倍。

PCI 共享总线在带宽和设备数量不断增长后迅速成为瓶颈。随着高速网卡、RAID 控制器以及 GPU 等高吞吐设备出现，共享总线逐渐无法满足需求，也为后来点对点高速互连（PCI Express）的出现埋下伏笔。

=== 历史影响
今天，无论是 Linux、Windows 还是 macOS，几乎所有现代操作系统都建立在虚拟内存之上。每个进程都拥有独立的虚拟地址空间，而 MMU、TLB 与 Page Table 已成为处理器不可缺少的组成部分。应用程序无需关心物理内存的位置，操作系统负责在有限的 DRAM、磁盘交换区以及高速缓存之间完成统一管理。

DMA 同样彻底改变了现代 I/O 架构。从 SSD、网卡到 GPU，几乎所有高性能外设都通过 DMA 直接与主存交换数据，CPU 更多地承担调度者而非搬运工的角色。今天 RDMA、NVMe、GPU Direct、零拷贝（Zero-copy）等技术，本质上都是这一思想的进一步发展——让数据尽可能绕过 CPU，减少无意义的数据搬运。